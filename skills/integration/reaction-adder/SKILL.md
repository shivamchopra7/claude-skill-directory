---
name: reaction-adder
description: Slack メッセージにリアクションを追加する。「リアクション追加」「リアクションつけて」「👍つけて」「絵文字で反応」「リアクションで返信」「いいねして」「リアクション送って」などで起動。公式Slack MCPの `slack_add_reaction` を使用。
allowed-tools: [FetchMcpResource]
---

# Reaction Adder

Slack メッセージにリアクション（絵文字）を追加します。

## ワークフロー

### 1. リアクション情報の確認

以下を確認:
- チャンネルID
- メッセージのタイムスタンプ
- 絵文字名（コロンなし）

### 2. リアクション追加

公式Slack MCPの `slack_add_reaction` ツールを使用:

```
slack_add_reaction(
  channel_id="C01234567",
  timestamp="1234567890.123456",
  emoji="thumbsup"
)
```

### 3. 結果の報告

リアクション追加の成功を報告

## よく使う絵文字

- `thumbsup` - 👍
- `heart` - ❤️
- `eyes` - 👀
- `fire` - 🔥
- `100` - 💯
- `tada` - 🎉
- `rocket` - 🚀

## コマンド連携

実際の処理は `/shiiman-slack:reaction-add` に委譲します（SSOT として扱う）。
