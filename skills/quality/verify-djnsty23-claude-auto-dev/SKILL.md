---
name: verify
description: Redirects to review deep. Use review with depth levels instead.
triggers:
  - verify
allowed-tools: Read
model: opus
user-invocable: false
disable-model-invocation: true
---

# Verify (Deprecated)

This skill has been merged into `review` with depth levels.

Use `review deep` for full 7-step verification with UI checks.
Use `review` for adaptive effort scaling (default).
Use `review quick` for build + typecheck only.
