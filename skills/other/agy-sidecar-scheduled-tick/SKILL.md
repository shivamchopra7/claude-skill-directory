---
name: agy-sidecar-scheduled-tick
description: "Run a recurring AgentOps loop tick on AGY via an Antigravity sidecar (the schedule builtin + agentapi), capturing per-fire agentapi runtime evidence a validator can read back."
---

# agy-sidecar-scheduled-tick (Codex)

This is the Codex-runtime entry point for the **agy-sidecar-scheduled-tick**
skill. The full methodology — the eight critical constraints, the four-phase
sidecar workflow, the per-fire evidence layout, output spec, rubric, examples,
and troubleshooting — lives in the sibling base skill:

- **Canonical skill:** [`../../skills/agy-sidecar-scheduled-tick/SKILL.md`](../../skills/agy-sidecar-scheduled-tick/SKILL.md)

Read that file first. Then follow the Codex Execution Profile in [`prompt.md`](./prompt.md) for the runtime-specific tool mapping and guardrails.
