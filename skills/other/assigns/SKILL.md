---
name: assigns
description: Compatibility alias for the Elixir/Phoenix plugin's LiveView assigns audit. Invoke explicitly with /lv:assigns.
disable-model-invocation: true
argument-hint: path/to/live_view.ex
---

# LiveView Assigns Compatibility Alias

Use the Skill tool to invoke `phx:assigns-audit` with the complete request
below, then follow that canonical skill exactly. Do not perform a separate
analysis before loading it.

$ARGUMENTS
