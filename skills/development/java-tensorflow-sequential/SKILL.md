---
id: "cd1f93c4-f99b-4a78-b91c-7f3bff5481e9"
name: "java / tensorflow / sequential"
description: "General SOP for common requests related to java, tensorflow, sequential."
version: "0.1.0"
tags:
  - "java"
  - "tensorflow"
  - "sequential"
triggers:
  - "Use when the user asks for a process or checklist."
  - "Use when you want to reuse a previously mentioned method/SOP."
examples:
  - input: "Break this into best-practice, executable steps."
---

# java / tensorflow / sequential

General SOP for common requests related to java, tensorflow, sequential.

## Prompt

Follow this SOP (replace specifics with placeholders like <PROJECT>/<ENV>/<VERSION>):
1) java.net.SocketException: 你的主机中的软件中止了一个已建立的连接
2) 打断点在执行这句话之前等待一段时间就好了
3) 而其他的minioClient操作都没有这种现象
4) 是因为我执行过这句话吗
5) public String createPathIfNotExist(String path
6) if (!minioClient.bucketExists(BucketExistsArgs.builder().bucket(path).build
7) minioClient.makeBucket(MakeBucketArgs.builder().bucket(path).build
8) return path
9) JSONObject.parseObject 可以忽略大小写吗
10) com.alibaba.fastjson2.JSONException: format null not support, input "2024-01-25 16:34:00.24", offset 99, character }, line 1, column 100, fastjson-version 2.0.43 {"trainconfigid":"7","filename":"\"model_up_40_ty_.h5\"","uploadtime":"\"2024-01-25 16:34:00.24

For each step, include: action, checks, and failure rollback/fallback plan.
Output format: for each step number, provide status/result and what to do next.

## Triggers

- Use when the user asks for a process or checklist.
- Use when you want to reuse a previously mentioned method/SOP.

## Examples

### Example 1

Input:

  Break this into best-practice, executable steps.
