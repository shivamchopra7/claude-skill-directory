---
name: enabler
description: 指定したプラグインを有効化する。「プラグインを有効化」「〇〇をオンにして」「〇〇を有効にして」「プラグインを有効に」「〇〇を使えるようにして」「プラグインをオンに」「〇〇を有効化して」などで起動。claude plugin enable コマンドを使用して有効化。
---

# Plugin Enabler

無効化されているプラグインを有効化します。

## ワークフロー

### 1. プラグイン名の確認

ユーザーがプラグイン名を指定していない場合、インストール済みプラグインの一覧を表示して選択を促す。

### 2. 有効化の実行

```bash
claude plugin enable [plugin-name]
```

### 3. 結果の報告

有効化の結果をユーザーに報告する。

## 出力フォーマット

```
## プラグインの有効化

プラグイン `shiiman-xxx` を有効化しています...

✅ 有効化完了

プラグイン `shiiman-xxx` が有効になりました。
```

## 重要な注意事項

- ✅ `claude plugin enable` コマンドを使用
- ✅ 有効化前にプラグインのインストール状態を確認
- ✅ 有効化結果をユーザーに報告
- ❌ インストールされていないプラグインを指定された場合はエラーメッセージを表示
- ❌ 既に有効なプラグインを指定された場合はその旨を通知
