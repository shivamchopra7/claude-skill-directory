# Discovery Learning Data

This directory stores the latest run snapshots used for discovery learning.

- `discovery_candidates.jsonl`: candidate-level discovery events (repo/path rows)
- `discovery_observations.jsonl`: download outcome observations from sharded fetch
- `discovery_priors.json`: rolling priors aggregated across runs

Files are regenerated by `.github/workflows/sync-data.yml`.
